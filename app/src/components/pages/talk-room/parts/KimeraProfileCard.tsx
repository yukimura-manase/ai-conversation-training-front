import { KimeraEntity } from "@/types/entitys/Kimera";

interface KimeraCardProps {
  kimeraId: string;
}

const KimeraProfileCard = ({}: KimeraCardProps) => {
  return (
    <div>
      <h1></h1>
    </div>
  );
};
